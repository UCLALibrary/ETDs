# ETDs
EtdsCopier.jpr is the JDeveloper project descriptor--useful for importing into new editions of JDev or onto new machines.

gdService is the subproject for the GradDiv release-date client.

scripts contains shell scripts and supporting config & sql files for processing ETDs.

src contains Java source code for utilities which handle more complex aspects of processing, like calculating dates
and updating XML.

